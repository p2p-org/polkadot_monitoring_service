version: '3.4'

services:
  calamari_exporter:
    build:
      context: ./exporters/manta
    environment:
    - "LISTEN=0.0.0.0"
    - "PORT=9150" 
    - "CHAIN=calamari"
    env_file:
    - ./manta.env
    networks:
    - exporters

